---
name: conflicts_resolver

on:
  workflow_dispatch:

jobs:
  resolve_conflicts:
    runs-on: ubuntu-latest
    steps:
      - name: resolve-test
        uses: Mergifyio/gha-versionining-conflict-resolver@main
        with:
          base: "main"
